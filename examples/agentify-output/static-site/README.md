# agentify output: static-site

This directory contains an example agent-readable bundle for `https://notes.example/`.

## Crawl result

Status: `complete`

Fetched: `3`

Failed: `0`

Warnings: none

## What this demonstrates

* rich metadata extraction
* successful route discovery
* complete crawl
* headings and descriptions carried into the route inventory

## Files

```txt
system.json
runtime.json
context.json
llms.txt
README.md
```

`runtime.json` shows the successful crawl receipt. `system.json` shows the discovered site shape and declared capabilities.
